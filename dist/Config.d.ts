import ConfigEngine from "./util/ConfigEngine";
export declare const engine: ConfigEngine<{
    TSCONFIG_PATH: string;
    MODEL_FILE_PATTERN: string;
    CACHE_FILE_PATTERN: string;
}>;
declare const Config: {
    TSCONFIG_PATH: string;
    MODEL_FILE_PATTERN: string;
    CACHE_FILE_PATTERN: string;
};
export default Config;
